name: 读取局部邻域复赛报告
on:
  push:
    branches: [workbench/skill-pt-matrix-distill]
    paths: ['.github/workflows/read-neighborhood-report-once.yml']
permissions: {actions: read, contents: write}
jobs:
  read:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: workbench/skill-pt-matrix-distill}
      - uses: actions/download-artifact@v4
        with:
          name: 3速1耐1智局部邻域报告
          path: /tmp/report
          repository: xf8410/umaai-rs
          run-id: 32665350897
          github-token: ${{ github.token }}
      - run: |
          cp /tmp/report/3速1耐1智局部邻域报告.md .trae/exports/3速1耐1智局部邻域报告.md
          rm .github/workflows/read-neighborhood-report-once.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m '保存3速1耐1智局部邻域摘要'
          git push origin HEAD:workbench/skill-pt-matrix-distill
